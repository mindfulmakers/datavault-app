[project]
name = "datavault-app-api"
version = "0.1.0"
description = ""
authors = []
dependencies = [
    
    
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project.entry-points."web.app_packages"]
datavault_app_api = "datavault_app_api.package_apps:get_apps"
